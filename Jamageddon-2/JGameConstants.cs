using Microsoft.Xna.Framework;

namespace Jamageddon2
{
    public static class JGameConstants
    {
        // Entity sizes
        public static readonly float DEFAULT_ENTITY_SIZE = 64f;
        public static readonly int SCREEN_WIDTH = 1280;
        public static readonly int SCREEN_HEIGHT = 720;

        // Tower sizes
        public static readonly float BIG_TOWER_SIZE = DEFAULT_ENTITY_SIZE;
        public static readonly float SMALL_TOWER_SIZE = DEFAULT_ENTITY_SIZE / 2;
    }
}